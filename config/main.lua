Shop, Services = {}, {}
config = require 'config.types'
locationsConfig = require 'config.locations'
categoriesConfig = require 'config.categories'
Customize = {
    Locale = 'en',
    ImagePath = nil,
    producerSupport = true, -- It redirects to the store page once to support the creator and does not repeat.
    ImagePathEnabled = true,
    Interaction = 'interact'
}